const HourHistory = require('../models/HourHistory')

class HourHistoryController {
	async store(req, res) {
		const { minutes } = req.body
		try {
		} catch (err) {}
	}
}

module.exports = new HourHistoryController()
